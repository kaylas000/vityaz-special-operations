# 🎨 VITYAZ GRAPHICS ENGINE

## 🂼 Обзор

Полнофункциональная система процедурной графики для игры **VITYAZ: Special Operations**.
Все спрайты и эффекты генерируются программно без необходимости в художниках.

### ✅ Что реализовано

- **40+ процедурных компонентов графики**
- **Символика отряда Витязь** (краповый берет на левой стороне ✅)
- **Система анимаций** с плавными переходами
- **Движок визуальных эффектов** (кровь, взрывы, вспышки)
- **Профессиональный UI/HUD** с тактическими элементами
- **Частичная система** для частиц
- **Полная интеграция** в Phaser.js

---

## 📂 Структура файлов

```
frontend/src/graphics/
├── ProceduralGraphics.ts          # 40+ процедурных компонентов
├── AnimationSystem.ts              # Система анимаций и кадров
├── VisualEffectsEngine.ts          # Эффекты: кровь, взрывы, дым
├── UIGraphicsEngine.ts             # HUD, меню, интерфейс
├── GraphicsIntegrationManager.ts   # Центральный координатор
└── README.md                       # Документация
```

---

## 🎮 ВЫТЯЗ СИМВОЛИКА (важно!)

### Краповый берет (CRAPOV BERET) ✅

- **Цвет**: Краповый (рыжевато-коричневый: `#6B3410`)
- **Позиция**: ЛЕВая сторона (индекс 0-3 пикселей в спрайте!)
- **Эмблема**: Золотой круг на берете (символ Витязя)
- **Реализация**: `ProceduralGraphics.drawPlayerUniform()`

```typescript
// Краповый берет на левой стороне
const beretColor = '#6B3410'; // Crapov (рыжевато-коричневый)
ctx.fillStyle = beretColor;
ctx.beginPath();
ctx.ellipse(3, 2, 3, 2, -0.3, 0, Math.PI * 2); // LEFT side!
ctx.fill();
```

### Шеврон Витязя (CHEVRON)

- **Форма**: Золотой ромб на груди
- **Цвет**: Золото `#FFD700`
- **Значение**: Отличительный знак спецподразделения

### Погоны (EPAULETTES)

- **Цвет**: Золотистые
- **Позиция**: На плечах
- **Назначение**: Военная символика

---

## 🛠️ ФАЗЫ РЕАЛИЗАЦИИ

### ✅ ФАЗА 1: BASE SPRITES (40+ компонентов)

**Проедурные компоненты:**

1. **Персонаж игрока** (18 компонентов)
   - `drawPlayerHead()` - голова с лицом
   - `drawPlayerBody()` - туловище с камуфляжем
   - `drawPlayerUniform()` - форма с **краповым беретом**
   - `drawPlayerLimbs()` - конечности с анимацией
   - `drawPlayerWeapon()` - оружие с поворотом

2. **Враги** (5 компонентов)
   - `drawBasicEnemy()` - базовый враг
   - `drawArmedEnemy()` - вооруженный враг
   - `drawHeavyEnemy()` - тяжелый враг/босс

3. **Эффекты** (6 компонентов)
   - `drawBloodSplat()` - кровавый брызг
   - `drawMuzzleFlash()` - вспышка из ствола
   - `drawExplosion()` - взрыв
   - `drawCasingSpark()` - искра гильзы
   - `drawHealthBar()` - полоса здоровья

### ✅ ФАЗА 2: ANIMATIONS (7+ анимаций)

**Встроенные анимации:**

- `player-walk` - ходьба (4 кадра)
- `player-attack` - атака со отдачей (3 кадра)
- `player-idle` - спокойствие
- `enemy-walk` - враг ходит (4 кадра)
- `enemy-attack` - враг атакует (2 кадра)
- `explosion` - взрыв (6 кадров)
- `death` - смерть/падение (3 кадра)

**Управление:**
```typescript
AnimationSystem.playAnimation(sprite, 'player-walk');
AnimationSystem.update(delta); // В main loop
```

### ✅ ФАЗА 3: VISUAL EFFECTS ENGINE

**Эффекты:**

- `bloodSplat()` - кровь на ударе
- `explosion()` - взрыв с волной
- `muzzleFlash()` - вспышка стрельбы
- `bulletImpact()` - удар пули
- `screenShake()` - тряска экрана
- `screenFlash()` - вспышка экрана

**Частичные системы:**
- Blood System (кровь)
- Spark System (искры)
- Smoke System (дым)
- Impact System (удар)

### ✅ ФАЗА 4: UI GRAPHICS ENGINE

**HUD элементы:**

- `createMainHUD()` - основная панель
- `createHealthBar()` - полоса здоровья с текстом
- `createAmmoCounter()` - счетчик боеприпасов
- `createWaveIndicator()` - индикатор волны
- `createWeaponStatus()` - статус оружия
- `createMinimap()` - миникарта
- `createCompass()` - компас
- `createScoreDisplay()` - счет и убийства

**Меню:**

- `createMainMenu()` - главное меню
- `createPauseMenu()` - меню паузы
- `createGameOverScreen()` - экран конца игры
- `createMenuButton()` - кнопка меню

**Уведомления:**

- `showDamageNumber()` - всплывающий урон
- `showNotification()` - уведомление на экран

### ✅ ФАЗА 5: INTEGRATION MANAGER

**Центральный координатор:**

- Управление всеми графическими системами
- Создание спрайтов персонажа и врагов
- Воспроизведение эффектов
- Синхронизация состояния игры с HUD
- Обновления в main loop

---

## 🚀 БЫСТРЫЙ СТАРТ (5 минут)

### 1. Импорт

```typescript
import GraphicsIntegrationManager from './graphics/GraphicsIntegrationManager';

// В вашей сцене Phaser
const graphicsManager = new GraphicsIntegrationManager(this);
```

### 2. Создание спрайтов

```typescript
// Игрок
const player = graphicsManager.createPlayerSprite(400, 300);

// Враги
const enemy1 = graphicsManager.createEnemySprite('enemy-1', 200, 150, 'armed');
const enemy2 = graphicsManager.createEnemySprite('enemy-2', 600, 150, 'heavy');
```

### 3. Воспроизведение эффектов

```typescript
// Выстрел
graphicsManager.playPlayerAttackAnimation(angleInRadians);

// Урон враге
graphicsManager.playEnemyDamageEffect('enemy-1', x, y, damageAmount);

// Смерть
graphicsManager.playEnemyDeathEffect('enemy-1', x, y);
```

### 4. Обновление HUD

```typescript
graphicsManager.updateGameState({
  health: 85,
  ammo: 28,
  score: 1250,
  kills: 12,
  wave: 3,
  enemiesRemaining: 5,
});
```

### 5. Update Loop

```typescript
// В функции update() сцены
update(delta) {
  graphicsManager.update(delta);
  // ... ваш код
}
```

---

## 📊 ПРОИЗВОДИТЕЛЬНОСТЬ

### Оптимизация

- ✅ Кэширование спрайтов
- ✅ Object pooling для частиц
- ✅ Процедурная генерация (нет больших файлов)
- ✅ Максимум 100+ спрайтов одновременно

### Требования

- **CPU**: Низкая (процедурная генерация один раз)
- **GPU**: Средняя (стандартные спрайты Phaser)
- **Память**: ~2-5MB для кэша спрайтов

---

## 🎨 КАСТОМИЗАЦИЯ

### Изменение цветов

```typescript
// В ProceduralGraphics.ts
static drawBasicEnemy() {
  // Измените DC143C на нужный цвет
  ctx.fillStyle = '#FF6600'; // Оранжевый враг
}
```

### Добавление новых компонентов

```typescript
// Добавьте новый метод в ProceduralGraphics
static drawCustomSprite(): HTMLCanvasElement {
  const canvas = document.createElement('canvas');
  canvas.width = 32;
  canvas.height = 32;
  const ctx = canvas.getContext('2d')!;
  
  // Ваш код рисования
  ctx.fillStyle = '#FF0000';
  ctx.fillRect(0, 0, 32, 32);
  
  return canvas;
}
```

### Новые анимации

```typescript
// В AnimationSystem.ts
private static buildAnimations(): void {
  this.addAnimation('custom-animation', {
    frames: this.generateCustomFrames(5),
    loop: true,
  });
}
```

---

## 🔧 ИНТЕГРАЦИЯ С PHASER

### Использование с физикой

```typescript
const playerSprite = graphicsManager.createPlayerSprite(400, 300);
const playerBody = this.physics.add.existing(playerSprite, true);
playerBody.setVelocity(100, 0);
```

### Использование с группами

```typescript
const enemyGroup = this.add.group();
for (let i = 0; i < 5; i++) {
  const enemy = graphicsManager.createEnemySprite(`enemy-${i}`, 100 + i * 50, 200);
  enemyGroup.add(enemy);
}
```

---

## 📈 ROADMAP

### Phase 1: ✅ ЗАВЕРШЕНО
- ✅ Процедурные спрайты
- ✅ Анимации
- ✅ Эффекты
- ✅ UI/HUD
- ✅ Интеграция

### Phase 2: 🚀 В ПЛАНАХ
- 🔄 Улучшение качества спрайтов
- 🔄 Больше типов врагов
- 🔄 Более сложные эффекты
- 🔄 Skin system для персонажа
- 🔄 Tema темы интерфейса

### Phase 3: 🎯 БУДУЩЕЕ
- 🎬 HD спрайты (если нанять художника)
- 🎵 Синхронизация звука с эффектами
- 🌍 Динамические карты
- 🎮 Множественные персонажи

---

## 📝 ВАЖНЫЕ КОММЕНТАРИИ

### О ВИТЯЗЕ

Внимание! Символика отряда Витязь включена правильно:

```
🎖️ КРАПОВЫЙ БЕРЕТ - на ЛЕВой стороне спрайта (не правой!)
💛 ЗОЛОТОЙ ШЕВРОН - на груди
⭐ ЭМБЛЕМА - мала на берете
```

Это исторически точная символика российского спецназа **ВИТЯЗЬ**.

### О ПРОИЗВОДИТЕЛЬНОСТИ

Все спрайты генерируются один раз и кэшируются, поэтому нет потерь производительности после первого кадра.

---

## 🤝 КОНТРИБЬЮТИНГ

Хотите улучшить графику? Вспомните:

1. Все новые компоненты должны быть в `ProceduralGraphics.ts`
2. Новые анимации в `AnimationSystem.ts`
3. Тестируйте на производительность
4. Документируйте ваш код

---

## 📞 ПОДДЕРЖКА

Вопросы? Проблемы?

- 📖 Читайте JSDoc комментарии в коде
- 🐛 Создавайте issue в GitHub
- 💬 Обсуждайте в discord

---

**Made with ❤️ for VITYAZ: Special Operations**

*Дата: 14 декабря 2025*
*Версия: 1.0.0*
